# direnv configuration for I Do Blueprint
# This file loads environment variables from .env.mcp.local

# Source the MCP environment variables
if [ -f .env.mcp.local ]; then
  source_env .env.mcp.local
else
  echo "Warning: .env.mcp.local not found"
fi
