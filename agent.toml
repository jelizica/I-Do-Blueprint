# agent.toml
# See https://docs.qodo.ai/qodo-documentation/qodo-command/features/creating-and-managing-agents
# This file configures the default agent that qodo command should use when no
# custom command (from agents/*.toml) is specified.

version = "1.0"

# Set Anthropic Claude 4.5 Opus (latest available frontier model) as the default LLM.
model = "claude-4.5-opus"

# Optional default instructions for general qodo commands (e.g., qodo chat).
instructions = """
You are the default Qodo CLI agent for the I Do Blueprint repository.
Follow the AGENTS.md guidelines, keep changes minimal and well-tested,
and prefer Swift best practices that match the existing architecture.
"""
